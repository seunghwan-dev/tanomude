# Copy to .env and fill real local values. Never commit .env.
# Credentials are placeholders here; .env holds the real local-dev values.
POSTGRES_USER=CHANGE_ME
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=mock_as400
DATABASE_URL=postgresql+psycopg://CHANGE_ME:CHANGE_ME@localhost:5432/mock_as400
